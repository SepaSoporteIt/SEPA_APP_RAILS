<p id="notice"><%= notice %></p>

<p>
  <h1><%= @visit_type.name %></h1>
</p>

<p>
  <strong>Descripcion:</strong>
  <%= @visit_type.description %>
</p>

<%= link_to 'Editar', edit_visit_type_path(@visit_type) %> |
<%= link_to 'Regresar al Listado', visit_types_path %>
