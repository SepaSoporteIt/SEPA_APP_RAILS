<p id="notice"><%= notice %></p>

<h1>Rubro/Actividad</h1>

        <%= link_to 'Nuevo Rubro/Actividad', new_industry_type_path %>
 </br>
 </br>       
<table class="pure-table pure-table-horizontal">
  <thead>
    <tr>
      <th>CIIU</th>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @industry_types.each do |industry_type| %>
      <tr>
        <td><%= industry_type.ciiu %></td>
        <td><%= industry_type.name %></td>
        <td><%= link_to 'Ver Detalles', industry_type %></td>
        <td><%= link_to 'Editar', edit_industry_type_path(industry_type) %></td>
        <td><%= link_to 'Eliminar', industry_type, method: :delete, data: { confirm: 'Esta Ud Seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


